# OS BoundaryLine data

## Install

Install [elixir](http://elixir-lang.org). E.g on the mac:

```sh
brew install elixir
```

## Create data

Create data:

```sh
make
```

Clean data:

```sh
make clean
```
